import 'package:flutter/material.dart';

class RegisterMahasiswaView extends StatelessWidget {
  const RegisterMahasiswaView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Register Mahasiswa"),
      ),
      body: Container(),
    );
  }
}
