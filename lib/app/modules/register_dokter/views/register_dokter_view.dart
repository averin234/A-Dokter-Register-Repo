import 'package:flutter/material.dart';

class RegisterDokterView extends StatelessWidget {
  const RegisterDokterView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Register Dokter"),
      ),
      body: Container(),
    );
  }
}
