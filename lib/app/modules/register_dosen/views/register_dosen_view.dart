import 'package:flutter/material.dart';

class RegisterDosenView extends StatelessWidget {
  const RegisterDosenView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Register Dosen"),
      ),
      body: Container(),
    );
  }
}
